{
  "private": true,
  "devDependencies": {
    "lerna": "^3.11.1"
  },
  "scripts": {
    "release:patch:dev": "lerna run build && lerna version patch --force-publish --no-git-tag-version && lerna run build && lerna publish from-package --dist-tag dev --yes"
  }
}
