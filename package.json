{
  "private": true,
  "devDependencies": {
    "lerna": "^3.11.1"
  },
  "scripts": {
    "verify-git-not-dirty": "git diff --quiet || exit 1",
    "release:patch": "npm run verify-git-not-dirty && lerna run build --stream && lerna version patch --yes && lerna run build --stream && git add . && git commit -m \"Patch version bump\" && lerna publish from-package --yes",
    "release:patch:dev": "npm run verify-git-not-dirty && lerna run build --stream && lerna version patch --no-git-tag-version --yes && lerna run build --stream && git add . && git commit -m \"Dev version bump\" && lerna publish from-package --dist-tag dev --yes",
    "release:prerelease:dev": "npm run verify-git-not-dirty && lerna run build --stream && lerna version prerelease --no-git-tag-version --yes && lerna run build --stream && git add . && git commit -m \"Dev version bump\" && lerna publish from-package --dist-tag dev --yes"
  }
}
