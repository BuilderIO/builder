// TODO: just add this to default lib too hm
interface Product {
  price: string;
}

// TODO: for now namespace
const getProductPrice = (product: Product) => {
  return product.price;
}

const fooBarBax = () => {
  return 'hi';
}

const yo = 'hi';
