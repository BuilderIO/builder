{
  "name": "@builder.io/react-tests",
  "description": "This mono-repo includes the new SDKs and their integration tests.",
  "private": true,
  "workspaces": [
    "react-vite",
    "nextjs"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -pt run build"
  },
  "engines": {
    "yarn": ">= 3.0.0"
  },
  "packageManager": "yarn@3.4.1"
}
