/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  experimental: {
    externalDir: true,
    appDir: true,
  },
};

// eslint-disable-next-line no-undef
module.exports = nextConfig;
