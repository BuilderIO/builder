# we need to publicly hoist `goober` to fix the e2e solid test.
# `goober` is imported by `solid-styled-components`, used in the SolidJS SDK.
# https://pnpm.io/npmrc#public-hoist-pattern
public-hoist-pattern[]=goober