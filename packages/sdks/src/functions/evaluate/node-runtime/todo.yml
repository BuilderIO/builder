my problem: `safeDynamicRequire` in NextJS projects is not working as expected.

possible setups:
- pages dir 
- pages dir + type:'module'
- app dir X
- app dir + type:'module'



steps:
- get app directory working with `'node:module'`